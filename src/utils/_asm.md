for integers:
  addition, subtraction and multiplication are identical for signed and unsigned, but division (and modulo) use different instructions
for floats on x64:
  floats are only implemented via SIMD registers
